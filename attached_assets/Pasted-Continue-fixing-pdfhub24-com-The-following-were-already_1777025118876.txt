Continue fixing pdfhub24.com. The following were 
already done — DO NOT redo them:

ALREADY DONE - SKIP THESE:
✅ Duplicate security headers removed
✅ Twitter meta tags added to index.html
✅ Organization schema improved with ImageObject
✅ noscript content fixed
✅ Copyright year fixed to 2026
✅ robots.txt updated with explicit Allow blocks
✅ Numbers fixed (1M+ consistent)
✅ Sitemap lastmod dates updated
✅ Twitter icon added to footer

NOW DO THESE REMAINING ITEMS:

═══════════════════════════════════════
## 1. SCHEMA MARKUP - MISSING SCHEMAS
═══════════════════════════════════════

Check each schema first. Only add if missing.

### WebApplication Schema
Add to EVERY tool page that is missing it:

{
  "@context": "https://schema.org",
  "@type": "WebApplication",
  "name": "[Tool Name]",
  "url": "https://pdfhub24.com/[tool-url]",
  "description": "[Tool Description]",
  "applicationCategory": "UtilitiesApplication",
  "operatingSystem": "Any",
  "browserRequirements": "Requires JavaScript",
  "offers": {
    "@type": "Offer",
    "price": "0",
    "priceCurrency": "USD"
  },
  "featureList": [
    "Free to use",
    "No registration required",
    "No watermarks",
    "Instant processing",
    "Secure file handling"
  ]
}

### FAQ Schema
Add to EVERY tool page that has an FAQ section
and is missing FAQ schema:

{
  "@context": "https://schema.org",
  "@type": "FAQPage",
  "mainEntity": [
    {
      "@type": "Question",
      "name": "[FAQ Question]",
      "acceptedAnswer": {
        "@type": "Answer",
        "text": "[FAQ Answer]"
      }
    }
  ]
}

Use the actual FAQ questions and answers 
already on each page. Do not invent new ones.

### HowTo Schema
Add to every tool page that has step by step 
instructions and is missing HowTo schema:

{
  "@context": "https://schema.org",
  "@type": "HowTo",
  "name": "How to [Tool Action] Online Free",
  "description": "[Tool description]",
  "step": [
    {
      "@type": "HowToStep",
      "position": 1,
      "name": "Upload Your File",
      "text": "Click Select File or drag and drop"
    },
    {
      "@type": "HowToStep",
      "position": 2,
      "name": "Configure Settings",
      "text": "Choose your preferred options"
    },
    {
      "@type": "HowToStep",
      "position": 3,
      "name": "Download Result",
      "text": "Process and download your file instantly"
    }
  ]
}

### BreadcrumbList Schema
Add to every tool and blog page missing it:

{
  "@context": "https://schema.org",
  "@type": "BreadcrumbList",
  "itemListElement": [
    {
      "@type": "ListItem",
      "position": 1,
      "name": "Home",
      "item": "https://pdfhub24.com"
    },
    {
      "@type": "ListItem",
      "position": 2,
      "name": "[Category Name]",
      "item": "https://pdfhub24.com/[category]"
    },
    {
      "@type": "ListItem",
      "position": 3,
      "name": "[Tool Name]",
      "item": "https://pdfhub24.com/[tool-url]"
    }
  ]
}

═══════════════════════════════════════
## 2. IMAGE OPTIMIZATION
═══════════════════════════════════════

1. Find ALL images and icons missing alt text
   Add descriptive alt text to each:
   - Tool icons: alt="[tool name] tool icon"
   - Trust badges: alt="[badge name]"
   - Logo: alt="PDF HUB 24 - Free Online PDF Tools"
   - Screenshots: alt="[description]"

2. Add width and height to all images 
   missing these attributes

3. Add loading="lazy" to all images 
   below the fold

4. Add loading="eager" to hero/logo images

═══════════════════════════════════════
## 3. PERFORMANCE - HEAD TAGS
═══════════════════════════════════════

Add to <head> if not already present.
Check for duplicates before adding.

Preconnect tags:
<link rel="preconnect" 
  href="https://fonts.googleapis.com" />
<link rel="preconnect" 
  href="https://fonts.gstatic.com" crossorigin />
<link rel="preconnect" 
  href="https://www.googletagmanager.com" />

DNS prefetch:
<link rel="dns-prefetch" 
  href="//fonts.googleapis.com" />
<link rel="dns-prefetch" 
  href="//www.google-analytics.com" />

Resource hints for most visited pages:
<link rel="prefetch" href="/compress-pdf" />
<link rel="prefetch" href="/pdf-to-word" />
<link rel="prefetch" href="/merge-pdf" />

Defer all non-critical scripts:
Find all <script src="..."> tags that are 
not in <head> critical path
Add defer attribute to each

═══════════════════════════════════════
## 4. UI FIXES
═══════════════════════════════════════

### Featured Badges
Remove "Featured" badge from ALL tool cards
Only keep on these 4 tools:
- Compress PDF
- PDF to Word
- Merge PDF
- Split PDF

### Unique Tool Icons
Give every tool a unique colored icon.
No two tools should use identical icon.
Conversion tools use format badge style.
Edit/utility tools use action icons.

### Back to Top Button
Add floating button:
- Only appears after 400px scroll down
- Fixed bottom-right (24px from edges)
- Blue circle with ↑ arrow
- Smooth scroll to top on click
- Fade in/fade out transition

### Navbar Search
Add 🔍 search icon to navbar:
- Opens full overlay on click
- Search input filters tools in real time
- Shows tool name + icon + link in results
- Closes on Escape key or outside click
- Shows "No tools found" if no match

### Fix Blank Gap
Find large empty white space between
"Ready to Transform Your PDFs?" section
and footer. Fix CSS so max gap is 40px.

### Dynamic Live Counter
Find hardcoded live user number.
Replace with:

(function() {
  var count = Math.floor(Math.random()*65)+85;
  var selectors = [
    '#live-counter',
    '[data-live-count]', 
    '.live-count',
    '.live-users'
  ];
  var el = null;
  for(var s of selectors) {
    el = document.querySelector(s);
    if(el) break;
  }
  if(el) {
    el.textContent = count;
    setInterval(function() {
      var delta = Math.floor(Math.random()*5)-2;
      count = Math.max(50, Math.min(200, count+delta));
      el.textContent = count;
    }, Math.floor(Math.random()*7000)+8000);
  }
})();

### Hide Empty Ad Placeholders
Find ALL empty advertisement divs.
Add display:none to hide them.
Do not show empty boxes to users.

### Testimonial Cards
- Remove large gray quotation marks
- Add small quote icon top-left instead
- Add colored left border to each card:
  Card 1 → #22C55E (green)
  Card 2 → #3B82F6 (blue)
  Card 3 → #8B5CF6 (purple)
  Card 4 → #F97316 (orange)
- Add initials avatar circle per reviewer:
  SM → green circle
  DL → blue circle
  JK → purple circle
  MR → orange circle
- Add below testimonials:
  "Enjoyed using PDF HUB 24? 
  Leave us a review on Trustpilot →"
  Link: https://trustpilot.com

═══════════════════════════════════════
## 5. DARK MODE VERIFICATION
═══════════════════════════════════════

Test every section in dark mode and fix 
any that look broken or unreadable:

- Navigation bar
- Hero section and upload box
- Tool cards grid
- Trust badges section
- How It Works steps
- Stats counter section
- Testimonials section
- FAQ section
- Footer

Ensure all text has sufficient contrast
in dark mode (min 4.5:1 ratio).

═══════════════════════════════════════
## 6. INTERNAL LINKING AUDIT
═══════════════════════════════════════

1. Check every tool page links to 
   at least 3 related tools

2. Check every blog post links to 
   at least 2 relevant tool pages

3. Find any orphan pages with no 
   internal links and add links to them

4. Verify breadcrumb navigation exists 
   on all tool and blog pages

5. Make sure homepage links to all 
   main tool categories

═══════════════════════════════════════
## 7. FINAL CHECK
═══════════════════════════════════════

After all changes:

1. List every file modified
2. Confirm no new duplicates introduced
3. Confirm all schema added correctly
4. Confirm UI changes applied
5. Confirm performance tags added
6. Deploy to Railway via GitHub push
7. Provide complete summary report